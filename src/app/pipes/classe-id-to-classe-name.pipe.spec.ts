import { ClasseIdToClasseNamePipe } from './classe-id-to-classe-name.pipe';

describe('ClasseIdToClasseNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ClasseIdToClasseNamePipe();
    expect(pipe).toBeTruthy();
  });
});
